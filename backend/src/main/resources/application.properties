# spring.application.name=backend

# jwt.secret=ChangeThisToAVeryLongAndSecureRandomStringPlease
# jwt.expiration-ms=3600000

# ==================================
# 1. MySQL 데이터베이스 연결 설정
# ==================================
# 🚨 'myjobdashboard' 부분은 3단계에서 생성할 DB(스키마) 이름입니다. 
#    원하는 이름으로 변경하셔도 됩니다.
#    (serverTimezone, characterEncoding 설정은 한글 깨짐 방지를 위해 권장됩니다.)
spring.datasource.url=jdbc:mysql://localhost:3306/myjobdashboard?serverTimezone=UTC&characterEncoding=UTF-8

# 🚨 'root' 대신 본인의 MySQL Workbench 사용자 이름을 입력하세요.
spring.datasource.username=root

# 🚨 '1234' 대신 본인의 MySQL Workbench 비밀번호를 입력하세요.
spring.datasource.password=jun081327!

# MySQL 드라이버 클래스 지정 (최신 버전에서는 자동 감지되지만, 명시하는 것이 좋습니다)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==================================
# 2. JPA (Hibernate) 설정
# ==================================
# (필수) MySQL의 문법(Dialect)을 사용하도록 설정
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# (필수) 애플리케이션 실행 시 JPA가 DB 테이블을 자동으로 처리하는 방식
#  - create: (주의!) 매번 실행 시 기존 테이블을 삭제하고 새로 만듦 (개발 초기)
#  - update: (권장) 엔티티 변경 사항만 DB 테이블에 반영 (개발 중)
#  - validate: 엔티티와 테이블이 일치하는지 검사만 함
#  - none: 아무것도 안 함 (운영)
spring.jpa.hibernate.ddl-auto=update

# (선택) JPA가 실행하는 SQL 쿼리를 콘솔 로그에 보여줍니다. (디버깅 시 유용)
spring.jpa.show-sql=true

# (선택) SQL 로그를 예쁘게 포맷팅
spring.jpa.properties.hibernate.format_sql=true

# 🚨 (필수!) JWT 토큰 서명을 위한 비밀 키입니다. 
# 아래 문자열을 그대로 쓰지 말고, 아무도 모르는 복잡하고 긴 문자열로 변경하세요!
jwt.secret=ChangeThisToAVeryLongAndSecureRandomStringPleaseForYourProjectSecurity

# 토큰 만료 시간 (1시간 = 3600000 밀리초)
jwt.expiration-ms=3600000

springdoc.swagger-ui.path=/swagger
